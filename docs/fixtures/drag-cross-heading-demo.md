# 跨标题拖动测试

## 章节 A

段落 A1：应可拖到章节 B 下。

## 章节 B

段落 B1：目标容器。

图片占位（无真实文件时跳过）：普通段落代替。

段落 B-子-1：可把章节 A 的段落拖到这里。

### 章节 B-子

段落 A2：拖动后保存，重开文档顺序应保持一致。

```mermaid
flowchart LR
  A[块1] --> B[块2]
```

```js
console.log("code block drag test");
```