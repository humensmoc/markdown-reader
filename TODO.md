# p0:

编辑体验

    基础使用体验参考obsidian和飞书

    刷新同步外部改动

    针对所有commonmark和GFM语法设计编辑状态

        比如各级标题和有序列表无序列表都需要输入完#或者-然后点击空格之后就自动将这一行渲染成对应标题等级的样式，并且在文本部分可以直接接鼠标点击将输入中光标放在对应的位置，可以直接打字输入

    修改标题时同步更新目录中的内容

模块拖动

    参考飞书，每行文字、标题以及所有子级内容、图片、mermaid语法解析出来的图、代码块都可以被拖动和摆放

# p1:

撤回体验

目录高亮逻辑：正文滚到哪才应该在目录上高亮下一个标题

mermaid可视化编辑

表头冻结在顶部，参考飞书。直到整个表表格最后一行移除正文部分的顶部边缘

metadata解析,比如tag，工作路径内的文档跳转（需要跳转到对应文本的标题位置）

自定义各种颜色标题大小

    json配置

上传到vscode商城

最宽宽度 

# done：

- cite跳转bug 

- 字体缩放 

- 折叠按钮显示、折叠按钮大小、左侧边距 

- 对应可读的基础要求和进阶要求spec文档（类似cite之类的） 

- mermaid渲染

  - 可以再优化样式和点进放大界面之后的滚轮缩放，左键拖动

- 目录滚动到头时再滚动不要滚动正文，否则正文动了，目录也会跟着动，影响滚动目录的结果

- 不要把文件名当作第一级标题，应该把#一个井号的当作第一级标题

- 支持文档内通过某种语法，比如%标题编号%跳转到对应的标题位置，然后解析成和cite差不多，然是不同的样式，而且也要能回到原文

- 正文可折叠，折叠正文时也折叠目录。但折叠目录的时候不要折叠正文、

- 会新开一个标签页的问题，应该公用一个标签页

- 支持obsidian的[[]]文件跳转语法

- 要不要点击所有md文档直接打开，还是优先把这个放在最上面

    不要点击md文档直接通过插件点开，用右键菜单打开，然后插件内跳转的新文档直接用插件打开